import styled from '@emotion/styled';
import { colors } from '../../../styles';

export const MenuItem = styled.p`
  font-size: 1.2rem;
  font-style: normal;
  font-weight: 400;
  color: ${colors.black};
`;
