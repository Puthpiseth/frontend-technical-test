{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // be explicit about types included
    // to avoid clashing with Jest types
    "allowJs": true,
    "types": ["cypress", "@testing-library/cypress"]
  },
  "include": ["../node_modules/cypress", "../tsconfig.json", "../package.json", "./**/*.ts"]
}
